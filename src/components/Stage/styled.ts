import styled from "styled-components";

export const StyledRootContainer = styled.div`
  background: #b0bec5;
  display: flex;
  justify-content: center;
  align-items: center;
  overflow: hidden;
  position: relative;
`;

export const SizeMonitor = styled.div`
  transition: transform 0.5s ease;
`;
